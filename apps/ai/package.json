{
  "name": "@tracelab/ai",
  "version": "0.0.1",
  "description": "TraceLab AI — Python FastAPI AI Microservice",
  "private": true,
  "scripts": {
    "dev": "uvicorn app.main:app --reload --host 0.0.0.0 --port 8000",
    "start": "uvicorn app.main:app --host 0.0.0.0 --port 8000",
    "test": "pytest",
    "lint": "ruff check app/",
    "format": "ruff format app/"
  }
}
